%canvas#canvas1{:width=>"800", :height => '600'}
%hr
%canvas#canvas2{:width=>"800", :height => '600'}
%hr
%canvas#canvas3{:width=>"800", :height => '600'}
%hr
%canvas#canvas4{:width=>"800", :height => '600'}
%hr
%canvas#canvas5{:width=>"800", :height => '600'}
%div{:id => "palette"}

:javascript
  $(function(){
    var names = _.keys(namedColors);
    for(var i = 0,j = colors.length; i < j; i++) {
      $('#palette').append("<div class=\"color\" data-color=\"" + names[i] + "\"></div>");
      $('#palette div.color:last').css('backgroundColor', "rgb(" + colors[i][0] + ", " + colors[i][1] + ", " + 
          colors[i][2] + ")");
    }
  
    $('#palette div.color').live('click', function(event) {
      b.penColor = $(event.target).attr('data-color');
    });

    b = new Brickifier('#canvas1');
    b.initialize('/images/carsonified.png');

    b1 = new Brickifier('#canvas2');
    b1.initialize('/images/dropbox.png');

    b2 = new Brickifier('#canvas3');
    b2.initialize('/images/html5.png');

    b3 = new Brickifier('#canvas4');
    b3.initialize('/images/ryancarson.jpg');

    b4 = new Brickifier('#canvas5');
    b4.initialize('/images/ferrari.jpg');

  })